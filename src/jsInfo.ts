export const name = 'CIT';

export const getName = () => {
    return `제 이름은 ${name}입니다.`;
}

export const getSum = (param: number) => {
    const number1 = 3;
    const number2 = 4;

    return number1 + number2 + param;
}

/*동작확인을 위한 주석*/
/*동작확인을 위한 주석2*/